class MediaStreamIndividual {
  constructor() {

  }

  /**
   * Return a random item of the given playlist
   *
   * @return PlaylistItem item Next item in playlist
   */
  getNext() {
    const item = this.playlist.next();

    // fetch from mediasource...

    return next();
  }
}

module.exports = MediaStreamIndividual;
